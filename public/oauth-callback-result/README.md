# Callback Result UI (Mercado Livre OAuth)

Frontend React/Vite da tela de confirmação de callback OAuth.

## Desenvolvimento

```bash
npm install
npm run dev
```

## Build de produção

```bash
npm run build
```

O build (`dist/`) é servido pelo backend principal na rota `/callback`, com dados dinâmicos injetados pelo servidor.
